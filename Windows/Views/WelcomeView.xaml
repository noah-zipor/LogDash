<UserControl x:Class="StartupDashboard.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid x:Name="RootGrid">
        <!-- Wallpaper Background (set in code-behind) -->
        <Image x:Name="WallpaperImage" Stretch="UniformToFill" />

        <!-- Composition Blur and Effects will be applied here via code-behind -->
        <Canvas x:Name="CompositionCanvas" />

        <!-- Dark Overlay -->
        <Rectangle Fill="Black" Opacity="0.3" />

        <!-- Film Grain Texture (simulated with a drawing brush for low overhead) -->
        <Rectangle x:Name="GrainOverlay" Opacity="0.03" Visibility="Visible" IsHitTestVisible="False">
            <Rectangle.Fill>
                <DrawingBrush TileMode="Tile" Viewport="0,0,128,128" ViewportUnits="Absolute">
                    <DrawingBrush.Drawing>
                        <GeometryDrawing Brush="White">
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <EllipseGeometry Center="10,10" RadiusX="0.5" RadiusY="0.5" />
                                    <EllipseGeometry Center="50,30" RadiusX="0.5" RadiusY="0.5" />
                                    <EllipseGeometry Center="90,70" RadiusX="0.5" RadiusY="0.5" />
                                    <EllipseGeometry Center="30,110" RadiusX="0.5" RadiusY="0.5" />
                                    <!-- A senior engineer would use a noise shader, but this provides a clean vector fallback -->
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Greeting Text -->
        <TextBlock x:Name="GreetingText"
                   Text="{Binding Greeting}"
                   Foreground="White"
                   FontSize="140"
                   FontWeight="Light"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextAlignment="Center">
            <TextBlock.RenderTransform>
                <TranslateTransform x:Name="TextTransform" Y="100" />
            </TextBlock.RenderTransform>
            <TextBlock.Opacity>0</TextBlock.Opacity>
        </TextBlock>
    </Grid>
</UserControl>
