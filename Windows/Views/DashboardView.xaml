<UserControl x:Class="StartupDashboard.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid>
        <Grid.Resources>
            <Storyboard x:Key="OnLoadedStoryboard">
                <DoubleAnimation Storyboard.TargetName="BackgroundWallpaper" Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" From="1.1" To="1.0" Duration="0:0:1.5">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation Storyboard.TargetName="BackgroundWallpaper" Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" From="1.1" To="1.0" Duration="0:0:1.5">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>

                <DoubleAnimation Storyboard.TargetName="TimePanel" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:1" />
                <DoubleAnimation Storyboard.TargetName="TimePanel" Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" From="50" To="0" Duration="0:0:0.8">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>

                <DoubleAnimation Storyboard.TargetName="AppsTitle" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:1" BeginTime="0:0:0.3" />

                <DoubleAnimation Storyboard.TargetName="NowPlayingPanel" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:1" BeginTime="0:0:0.5" />
                <DoubleAnimation Storyboard.TargetName="NowPlayingPanel" Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.X)" From="100" To="0" Duration="0:0:1" BeginTime="0:0:0.5">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </Grid.Resources>

        <UserControl.Triggers>
            <EventTrigger RoutedEvent="UserControl.Loaded">
                <BeginStoryboard Storyboard="{StaticResource OnLoadedStoryboard}" />
            </EventTrigger>
        </UserControl.Triggers>

        <!-- Background with blur and grain -->
        <Image x:Name="BackgroundWallpaper" Stretch="UniformToFill" RenderTransformOrigin="0.5,0.5">
            <Image.RenderTransform>
                <ScaleTransform ScaleX="1.1" ScaleY="1.1" />
            </Image.RenderTransform>
        </Image>
        <Rectangle Fill="Black" Opacity="0.5" />

        <Grid Margin="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <!-- Left Side: Time and Apps -->
            <StackPanel Grid.Column="0">
                <StackPanel x:Name="TimePanel" Margin="0,0,0,50" RenderTransformOrigin="0.5,0.5">
                    <StackPanel.RenderTransform>
                        <TranslateTransform />
                    </StackPanel.RenderTransform>
                    <TextBlock Text="{Binding CurrentTime}" FontSize="120" Foreground="White" FontWeight="Bold" />
                    <TextBlock Text="{Binding CurrentDate}" FontSize="32" Foreground="#AAFFFFFF" />
                </StackPanel>

                <!-- System Stats -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                    <Border Background="#11FFFFFF" CornerRadius="10" Padding="15,10" Margin="0,0,20,0">
                        <StackPanel>
                            <TextBlock Text="CPU" FontSize="12" Foreground="#88FFFFFF" FontWeight="Bold" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding SystemStats.CpuUsage, StringFormat={}{0:F0}}" FontSize="20" Foreground="White" FontFamily="Consolas" />
                                <TextBlock Text="%" FontSize="12" Foreground="#44FFFFFF" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Border Background="#11FFFFFF" CornerRadius="10" Padding="15,10">
                        <StackPanel>
                            <TextBlock Text="MEM" FontSize="12" Foreground="#88FFFFFF" FontWeight="Bold" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding SystemStats.MemoryUsage, StringFormat={}{0:F0}}" FontSize="20" Foreground="White" FontFamily="Consolas" />
                                <TextBlock Text="%" FontSize="12" Foreground="#44FFFFFF" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <TextBlock x:Name="AppsTitle" Text="Applications" Foreground="White" FontSize="24" Margin="0,0,0,20" />
                <ItemsControl ItemsSource="{Binding Apps}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Margin="10" Background="Transparent" BorderThickness="0" Command="{Binding DataContext.LaunchAppCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                <StackPanel Width="100">
                                    <Image Source="{Binding Icon, Converter={StaticResource ByteArrayToImageConverter}}" Width="64" Height="64" />
                                    <TextBlock Text="{Binding Name}" Foreground="White" TextAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,5,0,0" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- Right Side: Now Playing -->
            <StackPanel x:Name="NowPlayingPanel" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right" RenderTransformOrigin="0.5,0.5">
                <StackPanel.RenderTransform>
                    <TranslateTransform />
                </StackPanel.RenderTransform>
                <Border Background="#33FFFFFF" CornerRadius="20" Padding="20" Width="300">
                    <StackPanel>
                        <Image Source="{Binding NowPlaying.AlbumArt, Converter={StaticResource ByteArrayToImageConverter}}" Width="260" Height="260" Margin="0,0,0,15" />
                        <TextBlock Text="{Binding NowPlaying.Title}" Foreground="White" FontSize="18" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding NowPlaying.Artist}" Foreground="#AAFFFFFF" FontSize="14" />
                        <TextBlock Text="{Binding NowPlaying.IsPlaying, Converter={StaticResource PlaybackStateConverter}}" Foreground="#88FFFFFF" FontSize="12" Margin="0,10,0,0" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
